{{>header}}

{{#if imports}}

{{#each imports}}
import type { {{{name}}}{{#if alias}} as {{{alias}}}{{/if}} } from './{{{path}}}';
{{/each}}
{{/if}}

{{#equals export 'interface'}}
{{>exportInterface}}
{{else equals export 'one-of'}}
{{>exportComposition}}
{{else equals export 'any-of'}}
{{>exportComposition}}
{{else equals export 'all-of'}}
{{>exportComposition}}
{{else equals export 'enum'}}
{{>exportEnum}}
{{else}}
{{>exportType}}
{{/equals}}
