{{#if parameters}}
{{#if @root.useOptions~}}
{
{{#each parameters}}
{{{name}}},
{{/each}}
}: {
{{#each parameters}}
{{{name}}}{{>isRequiredDefinition}}: {{>type}},
{{/each}}
}
{{~else}}

{{#each parameters}}
{{{name}}}{{>isRequiredDefinition}}: {{>type}},
{{/each}}
{{/if}}
{{/if}}
