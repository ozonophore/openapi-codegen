{{#if parameters}}
{{#if @root.useOptions~}}
{
{{#each parameters}}
{{name}}{{#if default}} = {{default}}{{/if}}{{#unless @last}},{{/unless}}
{{/each}}
}: {
{{#each parameters}}
{{#if description}}
/**
 * {{description}}
 */
{{/if}}
{{name}}{{>isRequired}}: {{>type}}{{#unless @last}},{{/unless}}
{{/each}}
}
{{~else}}

{{#each parameters}}
{{name}}{{>isRequired}}: {{>type}}{{#if default}} = {{default}}{{/if}}{{#unless @last}},{{/unless}}
{{/each}}
{{~/if}}
{{/if}}
