{{#if parameters}}
{{#if @root.useOptions~}}
{
{{#each parameters}}
{{#containsSystemName name}}
/**
* [WARN] Be careful! A reserved system word is used!
*/
{{/containsSystemName}}
{{{name}}},
{{/each}}
}: {
{{#each parameters}}
{{{name}}}{{>isRequiredDefinition}}: {{>type}},
{{/each}}
}
{{~else}}

{{#each parameters}}
{{#containsSystemName name}}
/**
* [WARN] Be careful! A reserved system word is used!
*/
{{/containsSystemName}}
{{{name}}}{{>isRequiredDefinition}}: {{>type}},
{{/each}}
{{/if}}
{{/if}}
