{
    type: 'array',
    items: {{#if link}}{{#isBasicType link}}{{>jsonschema/jsonschemaSchemaGeneric link}}{{else}}{{{link.type}}}Schema{{/isBasicType}}{{else}}{{>jsonschema/jsonschemaSchemaGeneric}}{{/if}},
{{#if minItems}}
    minItems: {{{minItems}}},
{{/if}}
{{#if maxItems}}
    maxItems: {{{maxItems}}},
{{/if}}
{{#if uniqueItems}}
    uniqueItems: {{{uniqueItems}}},
{{/if}}
{{#if isNullable}}
    nullable: true,
{{/if}}
}