{{#equals export 'one-of'}}
Joi.alternatives().try(
{{#each properties}}
    {{>joi/joiSchema}}{{#unless @last}},{{/unless}}
{{/each}}
){{#if isNullable}}.allow(null){{/if}}
{{else equals export 'any-of'}}
Joi.alternatives().try(
{{#each properties}}
    {{>joi/joiSchema}}{{#unless @last}},{{/unless}}
{{/each}}
){{#if isNullable}}.allow(null){{/if}}
{{else equals export 'all-of'}}
{{#each properties}}
{{#if @first}}
{{>joi/joiSchema}}
{{else}}
.concat({{>joi/joiSchema}})
{{/if}}
{{/each}}{{#if isNullable}}.allow(null){{/if}}
{{/equals}}