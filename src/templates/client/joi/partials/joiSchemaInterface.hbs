Joi.object({
{{#if properties}}
{{#each properties}}
    {{{name}}}: {{>joiSchema}}{{#unless isRequired}}.optional(){{/unless}}{{#unless @last}},{{/unless}}
{{/each}}
{{else}}
})
{{/if}}
}){{#if isNullable}}.allow(null){{/if}}