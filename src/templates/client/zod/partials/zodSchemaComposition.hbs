{{#equals export 'one-of'}}
z.union([{{#each properties}}{{>zod/zodSchema}}{{#unless @last}}, {{/unless}}{{/each}}]){{#if isNullable}}.nullable(){{/if}}
{{else equals export 'any-of'}}
z.union([{{#each properties}}{{>zod/zodSchema}}{{#unless @last}}, {{/unless}}{{/each}}]){{#if isNullable}}.nullable(){{/if}}
{{else equals export 'all-of'}}
{{#each properties}}{{#if @first}}{{>zod/zodSchema}}{{else}}.and({{>zod/zodSchema}}){{/if}}{{/each}}{{#if isNullable}}.nullable(){{/if}}
{{/equals}}
