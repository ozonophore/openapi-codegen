{{#equals export 'one-of'}}
z.union([{{#each properties}}{{>zodSchema}}{{#unless @last}}, {{/unless}}{{/each}}]){{#if isNullable}}.nullable(){{/if}}
{{else equals export 'any-of'}}
z.union([{{#each properties}}{{>zodSchema}}{{#unless @last}}, {{/unless}}{{/each}}]){{#if isNullable}}.nullable(){{/if}}
{{else equals export 'all-of'}}
z.intersection({{#each properties}}{{>zodSchema}}{{#unless @last}}, {{/unless}}{{/each}}){{#if isNullable}}.nullable(){{/if}}
{{/equals}}