## 1. Implement

- [x] 1.1 `GenerationRootOptions` + resolve `{ items, root }`
- [x] 1.2 Rewire OpenApiClient, GenerationBatchSession, finalizeGenerationBatch
- [x] 1.3 Tests (golden `.items` + root assert) + tsc / knip / validate / graphify
