/* istanbul ignore file */
/* tslint:disable */
/* eslint-disable */

import { IModelWithStringSchema } from './ModelWithString';

import Ajv from 'ajv';
import addFormats from 'ajv-formats';

const ajv = new Ajv({ allErrors: true });
addFormats(ajv);

export const TArrayWithArraySchema = {
    type: 'array',
    items: false,
};

export type TArrayWithArray = any; // JSON Schema doesn't provide type inference

export function validateTArrayWithArray(
    data: unknown,
): { valid: true; data: TArrayWithArray } | { valid: false; errors: Ajv.ErrorObject[] } {
    const validate = ajv.compile(TArrayWithArraySchema);
    const valid = validate(data);
    if (valid) {
        return { valid: true, data: data as TArrayWithArray };
    }
    return { valid: false, errors: validate.errors || [] };
}

export function validateTArrayWithArraySync(data: unknown): TArrayWithArray {
    const validate = ajv.compile(TArrayWithArraySchema);
    const valid = validate(data);
    if (!valid) {
        throw new Error(ajv.errorsText(validate.errors));
    }
    return data as TArrayWithArray;
}
