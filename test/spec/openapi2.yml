openapi: "3.0.0"
info:
  title: Request for deposit path
  version: 0.0.2
  description: OpenAPI Specification

paths:
  /public/path/path1/create:
    post:
      operationId: createProcedure
      requestBody:
        content:
          application/json:
            schema:
              $ref: path/path1/create/createRequestDTO.json
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: path/path1/create/createResponseDTO.json

